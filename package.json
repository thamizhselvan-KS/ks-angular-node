{
    "name": "my-full-stack-app",
    "version": "1.0.0",
    "scripts": {
      "start": "(cd ./client) && npm start",
      "build": "cd angular-project && ng build --prod",
      "start:dev": "concurrently \"ng serve --port 7000\" \"(cd ./server) && npm start\"",
      "start:server": "node server.js",
      "start:client": "cd angular-project && ng serve",
      "build:client": "cd angular-project && ng build --prod",
      "test:client": "cd angular-project && ng test",
      "lint:client": "cd angular-project && ng lint",
      "test:server": "cd nodejs-project && npm test",
      "lint:server": "cd nodejs-project && npm run lint"
    },
    "dependencies": {
    },
    "devDependencies": {
    }
  }